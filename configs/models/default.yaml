providers:
  mock:
    type: mock
    deterministic: true
  ollama:
    type: ollama
    base_url: http://127.0.0.1:11434
  openai_compatible:
    type: openai_compatible
    base_url: http://127.0.0.1:1234/v1
    api_key_env: AGENTARIUM_OPENAI_API_KEY

resource_scheduler:
  max_concurrent_model_calls: 1
  max_ready_tasks: 8
